package main

import "fmt"

func main() {
	a := "stored in a"
	// b := "stored in b"
	fmt.Println("a -", a)
	// b is not being used - invalid code
	// commented out offending variable to get rid of red in IDE
}
